global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: api
    static_configs:
      - targets:
          - api:8080
    metrics_path: /metrics
    # Preserve metric labels emitted by prometheus-net (AC-001 — data fidelity)
    honor_labels: true
