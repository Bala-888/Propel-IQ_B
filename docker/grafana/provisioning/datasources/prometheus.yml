apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # uid must be stable so dashboard panels can bind by uid reference
    uid: prometheus
    # Internal Docker Compose hostname — NOT localhost (Edge: datasource provisioning failure)
    url: http://prometheus:9090
    access: proxy
    isDefault: true
