apiVersion: 1

providers:
  - name: Default
    folder: ''
    type: file
    options:
      # docker-compose.yml bind-mounts provisioning/dashboards/ to this path
      path: /var/lib/grafana/dashboards
