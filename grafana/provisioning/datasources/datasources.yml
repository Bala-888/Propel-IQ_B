apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false

  - name: Seq
    type: grafana-opensearch-datasource
    access: proxy
    url: http://seq:5341
    editable: false
